peace_ai_desires = {
    # Make specific tags always take states
    always_annex_for_tags = {
        peace_action_type = take_states
        
        enable = {
            ROOT = {
                OR = {
                    tag = AFR
                    tag = NAM
                    tag = SSA
                    tag = ASA
                    tag = EUR
                    tag = OCE
                    always = yes
                }
            }
        }
        
        ai_desire = 1000
    }
    
    # Prevent tags from puppeting
    never_puppet_for_tags = {
        peace_action_type = puppet
        
        enable = {
            ROOT = {
                OR = {
                    tag = AFR
                    tag = NAM
                    tag = SSA
                    tag = ASA
                    tag = EUR
                    tag = OCE
                    always = yes
                }
            }
        }
        
        ai_desire = -1000
    }
    
    # Prevent tags from liberating
    never_liberate_for_tags = {
        peace_action_type = liberate
        
        enable = {
            ROOT = {
                OR = {
                    tag = AFR
                    tag = NAM
                    tag = SSA
                    tag = ASA
                    tag = EUR
                    tag = OCE
                    always = yes
                }
            }
        }
        
        ai_desire = -1000
    }
}
